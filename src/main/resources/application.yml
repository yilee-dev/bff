server:
  port: 8080
  address: 10.117.9.40

spring:
  data:
    redis:
      host: 10.100.104.24
      port: 6400
      password: DHD@ngh222
  session:
    data:
      redis:
        namespace: "session"
        save-mode: always
    timeout: 30m

  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: http://10.100.104.24:8080/realms/donghee
        registration:
          keycloak:
            client-id: api-gateway
            client-secret: i4BIMYcDHdaFwcj19fFVHOksgg2Rrrqw
            authorization-grant-type: authorization_code
            scope:
              - openid
              - email
              - roles
              - profile
      resourceserver:
        jwt:
          issuer-uri: http://10.100.104.24:8080/realms/donghee
  cloud:
    gateway:
      server:
        webflux:
          default-filters:
            - TokenRelay
          routes:
            - id: rentals
              uri: http://10.117.9.40:8085
              predicates:
                - Path=/api/**
keycloak:
  admin:
    server-url: http://10.100.104.24:8080
    realm: donghee
    # api-gateway 클라이언트에서 Service accounts enabled + realm-management:view-users 권한 필요.
    # 별도 admin 전용 클라이언트가 있다면 그 client-id/secret으로 교체하세요.
    client-id: api-gateway
    client-secret: i4BIMYcDHdaFwcj19fFVHOksgg2Rrrqw

logging:
  level:
    org.springframework.security.web: trace
